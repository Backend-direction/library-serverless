import { TextField } from '@mui/material';

const SearchBar = () => {
  return (
    <TextField
      id="filled-search"
      label="Search field"
      type="search"
      variant="filled"
    />
  );
}
 
export default SearchBar;